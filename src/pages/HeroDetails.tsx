import {useParams} from "react-router-dom";
import type {HeroType} from "../@types/HeroType";

const HeroDetails = () => {

    return <h1>Détails du héros {hero.name}</h1>;
};

export default HeroDetails;